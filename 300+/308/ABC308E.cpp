#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main(){
  ll n;
  string s;
  cin >> n;
  ll A[n];
  for(ll i=0; i<n; i++){
    cin >> A[i];
  }
  cin >> s;
  // solve how to find the mex-es
  // then just use OR and lowest bit to find the mex answer
  return 0;
}